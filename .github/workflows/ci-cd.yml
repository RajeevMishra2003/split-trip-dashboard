name: Salesforce LWC + Apex CI/CD

on:
  push:
    branches:
      - main
    paths:
      - "force-app/main/default/lwc/**"
      - "force-app/main/default/classes/**"

  pull_request:
    branches:
      - main
    paths:
      - "force-app/main/default/lwc/**"
      - "force-app/main/default/classes/**"

jobs:
  test-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # =====================================================
      # 1. Checkout Repository
      # =====================================================
      - name: Checkout Repository
        uses: actions/checkout@v4

      # =====================================================
      # 2. Install Salesforce CLI
      # =====================================================
      - name: Install Salesforce CLI
        run: |
          npm install --global @salesforce/cli@latest

      # =====================================================
      # 3. Verify Salesforce CLI
      # =====================================================
      - name: Verify Salesforce CLI
        run: |
          node --version
          npm --version
          sf --version
          which sf

      # =====================================================
      # 4. Create JWT Private Key
      # =====================================================
      - name: Create JWT Key
        run: |
          printf '%s\n' '${{ secrets.SF_JWT_KEY }}' > server.key
          chmod 600 server.key

      # =====================================================
      # 5. Authenticate Salesforce using JWT
      # =====================================================
      - name: Authenticate Salesforce
        run: |
          sf org login jwt \
            --client-id "${{ secrets.SF_CLIENT_ID }}" \
            --jwt-key-file server.key \
            --username "${{ secrets.SF_USERNAME }}" \
            --instance-url "https://login.salesforce.com" \
            --alias MyDevOrg

      # =====================================================
      # 6. Verify Salesforce Authentication
      # =====================================================
      - name: Verify Salesforce Org
        run: |
          sf org display --target-org MyDevOrg

      # =====================================================
      # 7. Validate Deployment + RunLocalTests
      # =====================================================
      - name: Validate Salesforce Deployment
        run: |
          sf project deploy start \
            --source-dir force-app \
            --target-org MyDevOrg \
            --test-level RunLocalTests \
            --wait 30 \
            --dry-run

      # =====================================================
      # 8. Actual Deployment
      # =====================================================
      - name: Deploy to Salesforce
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          sf project deploy start \
            --source-dir force-app \
            --target-org MyDevOrg \
            --test-level RunLocalTests \
            --wait 30

      # =====================================================
      # 9. Cleanup
      # =====================================================
      - name: Cleanup
        if: always()
        run: |
          rm -f server.key
