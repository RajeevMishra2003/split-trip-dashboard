name: Salesforce LWC + Apex CI/CD

on:
  push:
    branches:
      - main
    paths:
      - "force-app/main/default/lwc/**"
      - "force-app/main/default/classes/**"

  pull_request:
    branches:
      - main
    paths:
      - "force-app/main/default/lwc/**"
      - "force-app/main/default/classes/**"

jobs:
  test-and-deploy:
    name: Test and Deploy
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
      # =====================================================
      # 1. Checkout
      # =====================================================
      - name: Checkout Repository
        uses: actions/checkout@v4

      # =====================================================
      # 2. Setup Node.js
      # =====================================================
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      # =====================================================
      # 3. Install Salesforce CLI
      # =====================================================
      - name: Install Salesforce CLI
        run: |
          npm install --global @salesforce/cli
          sf --version

      # =====================================================
      # 4. Verify Runtime
      # =====================================================
      - name: Verify Runtime
        run: |
          echo "Node:"
          node --version

          echo "NPM:"
          npm --version

          echo "Salesforce CLI:"
          sf --version

          echo "Salesforce CLI path:"
          which sf

      # =====================================================
      # 5. Create JWT Private Key
      # =====================================================
      - name: Create JWT Key
        shell: bash
        run: |
          printf '%s\n' '${{ secrets.SF_JWT_KEY }}' > server.key
          chmod 600 server.key

          echo "JWT key created successfully"

      # =====================================================
      # 6. Authenticate Salesforce
      # =====================================================
      - name: Authenticate Salesforce
        shell: bash
        run: |
          sf org login jwt \
            --client-id "${{ secrets.SF_CONSUMER_KEY }}" \
            --jwt-key-file server.key \
            --username "${{ secrets.SF_USERNAME }}" \
            --instance-url "https://login.salesforce.com" \
            --alias MyDevOrg

      # =====================================================
      # 7. Verify Salesforce Org
      # =====================================================
      - name: Verify Salesforce Org
        run: |
          sf org display \
            --target-org MyDevOrg

      # =====================================================
      # 8. Validate + Run Local Tests
      # =====================================================
      - name: Validate Salesforce Deployment
        run: |
          sf project deploy start \
            --source-dir force-app \
            --target-org MyDevOrg \
            --test-level RunLocalTests \
            --wait 30 \
            --dry-run

      # =====================================================
      # 9. Actual Deployment
      # =====================================================
      - name: Deploy to Salesforce
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          sf project deploy start \
            --source-dir force-app \
            --target-org MyDevOrg \
            --test-level RunLocalTests \
            --wait 30

      # =====================================================
      # 10. Cleanup
      # =====================================================
      - name: Cleanup JWT Key
        if: always()
        run: |
          rm -f server.key
